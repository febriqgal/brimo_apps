import 'package:flutter/material.dart';

class BrivaPage extends StatelessWidget {
  const BrivaPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Briva',
      ),
    );
  }
}
